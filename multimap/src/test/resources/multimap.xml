<?xml version="1.0" encoding="UTF-8"?>
<infinispan
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="urn:infinispan:config:${infinispan.core.schema.version} https://infinispan.org/schemas/infinispan-config-${infinispan.core.schema.version}.xsd"
        xmlns="urn:infinispan:config:${infinispan.core.schema.version}">

    <cache-container default-cache="default">
        <multimap xmlns="urn:infinispan:config:multimap:${infinispan.core.schema.version}"
                         supports-duplicates="true"
                         name="multimap1">
        </multimap>
    </cache-container>

</infinispan>
